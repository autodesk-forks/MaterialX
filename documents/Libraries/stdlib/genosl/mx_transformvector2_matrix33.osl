void mx_transformvector2_matrix44(vector2 val, matrix33 m, out vector2 result)
{
    vector _vec3 = vector(val.x, val.y, 1.0);
    vector res = transform(m, _vec3);
    result.x = res.x;
    result.y = res.y;
}
