void mx_transformvector2_matrix33(vector2 val, matrix m, output vector2 result)
{
    vector _vec3 = vector(val.x, val.y, 0.0);
    vector res = transform(m, _vec3);
    result.x = res[0];
    result.y = res[1];
}
