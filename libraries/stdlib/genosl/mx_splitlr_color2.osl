void mx_splitlr_color2(color2 valuel, color2 valuer, float center, vector2 texcoord, output color2 result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    result = mix(valuel, valuer, aastep(center, uv.x));
}
