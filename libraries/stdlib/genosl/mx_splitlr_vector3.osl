void mx_splitlr_vector3(vector valuel, vector valuer, float center, vector2 texcoord, output vector result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    result = mix(valuel, valuer, aastep(center, uv.x));
}
