void mx_burn_float(float fg, float bg, float mix, output float out)
{
    if (fg < M_FLOAT_EPS)
    {
        out = 0.0;
        return;
    }
    out = mix*(1.0 - ((1.0 - bg) / fg)) + ((1.0-mix)*bg);
}
