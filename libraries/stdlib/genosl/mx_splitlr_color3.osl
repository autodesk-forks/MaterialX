void mx_splitlr_color3(color valuel, color valuer, float center, vector2 texcoord, output color result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    result = mix(valuel, valuer, aastep(center, uv.x));
}
