void mx_splitlr_vector4(vector4 valuel, vector4 valuer, float center, vector2 texcoord, output vector4 result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    result = mix(valuel, valuer, aastep(center, uv.x));
}
