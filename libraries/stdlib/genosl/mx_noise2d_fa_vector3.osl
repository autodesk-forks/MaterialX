void mx_noise2d_fa_vector3(float amplitude, float pivot, vector2 texcoord, output vector result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    vector value = noise("snoise", uv.x, uv.y);
    result = value * amplitude + pivot;
}
