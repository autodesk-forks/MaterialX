void mx_splittb_color2(color2 valuet, color2 valueb, float center, vector2 texcoord, output color2 result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    result = mix(valuet, valueb, aastep(center, uv.y));
}
