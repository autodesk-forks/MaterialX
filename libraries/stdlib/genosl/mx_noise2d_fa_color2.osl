void mx_noise2d_fa_color2(float amplitude, float pivot, vector2 texcoord, output color2 result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    color2 value = noise("snoise", uv.x, uv.y);
    result = value * amplitude + pivot;
}
