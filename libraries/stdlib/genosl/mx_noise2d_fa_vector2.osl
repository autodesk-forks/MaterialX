void mx_noise2d_fa_vector2(float amplitude, float pivot, vector2 texcoord, output vector2 result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    vector2 value = noise("snoise", uv.x, uv.y);
    result = value * amplitude + pivot;
}
