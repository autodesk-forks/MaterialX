void mx_splittb_vector3(vector valuet, vector valueb, float center, vector2 texcoord, output vector result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    result = mix(valuet, valueb, aastep(center, uv.y));
}
