void mx_noise2d_fa_vector4(float amplitude, float pivot, vector2 texcoord, output vector4 result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    vector4 value = noise("snoise", uv.x, uv.y);
    result = value * amplitude + pivot;
}
