void mx_splittb_color3(color valuet, color valueb, float center, vector2 texcoord, output color result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    result = mix(valuet, valueb, aastep(center, uv.y));
}
