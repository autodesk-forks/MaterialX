void mx_splitlr_vector2(vector2 valuel, vector2 valuer, float center, vector2 texcoord, output vector2 result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    result = mix(valuel, valuer, aastep(center, uv.x));
}
