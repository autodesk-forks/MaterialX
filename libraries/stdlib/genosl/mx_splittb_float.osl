void mx_splittb_float(float valuet, float valueb, float center, vector2 texcoord, output float result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    result = mix(valuet, valueb, aastep(center, uv.y));
}
