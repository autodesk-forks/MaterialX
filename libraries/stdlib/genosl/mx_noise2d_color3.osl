void mx_noise2d_color3(vector amplitude, float pivot, vector2 texcoord, output color result)
{
    vector2 uv = mx_get_target_uv(texcoord);
    color value = noise("snoise", uv.x, uv.y);
    result = value * amplitude + pivot;
}
