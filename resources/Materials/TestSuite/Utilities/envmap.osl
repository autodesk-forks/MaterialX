shader envmap(float Kb = 1, string filename = "")
{
   // This is pure specular reflection lookup. Needs to be replaced
   vector R = reflect (I, N);
   color c = environment(filename, R);
   Ci = Kb * c * background();
}
